@model IEnumerable<BookTitles.Models.Book_Author>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Author)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ISBN_Number)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Author.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ISBN_Number)
            </td>
            <td>
               
                @Html.ActionLink("Details", "Details", new { id=item.ISBN_Number, id2=item.AuthorID }) |
                @Html.ActionLink("Delete", "Delete", new {id=item.ISBN_Number, id2=item.AuthorID})
            </td>
        </tr>
}
    </tbody>
</table>
